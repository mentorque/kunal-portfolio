/**
 * Site contact — update `CONTACT_EMAIL` when needed.
 * Resume PDF lives in `public/`; update `RESUME_PATH` if the filename changes.
 */
export const CONTACT_EMAIL = "kunal.sonawane1101@gmail.com";

/** Public URL for resume PDF */
export const RESUME_PATH = `/${encodeURIComponent("kunal_sonawane_mentorque (3).pdf")}`;